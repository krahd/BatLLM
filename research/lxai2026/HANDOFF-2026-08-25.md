# LXAI 2026 experiment handoff

**Branch:** `lxai-rioplatense-experiment`  
**State:** frozen 576-call main run completed locally; exact result directory still needs to be rebased, committed and pushed.  
**Paper owner:** `krahd/research/academic-writing/my_papers_2026/2026 - LXAI NeurIPS 2026/`.

## Frozen main run

Runner:

```bash
python research/lxai2026/run_decision_experiment.py \
  --models \
  mistral-small3.2:24b-instruct-2506-q4_K_M \
  qwen3:30b-a3b-instruct-2507-q4_K_M \
  qwen3:4b-instruct-2507-q4_K_M \
  llama3.2:latest
```

Design: 48 cases × 3 language conditions × 4 models = 576 calls.

Do not change the suite, prompt, expected commands, models, direct transport, generation options, scoring or D1–D6 assignments for the reported main dataset.

## Completed run

Runner-reported local output directory:

`research/lxai2026/results/20260825T094526Z/`

Console summary:

- 576 calls accounted for;
- 0 provider errors;
- 217 strict command correct;
- 352 action errors;
- 7 invalid unrecoverable;
- 0 format-only;
- 0 format + action errors.

Primary matched Spanish comparison across 192 case/model pairs:

- standardised Spanish: 68/192 = 35.42%;
- Rioplatense: 71/192 = 36.98%;
- standard minus Rioplatense: −1.5625 percentage points;
- standard-only correct: 9;
- Rioplatense-only correct: 12;
- exact McNemar p = 0.6636238098144531.

Per model:

| Model | English | standard Spanish | Rioplatense | standard − Rio |
|---|---:|---:|---:|---:|
| Mistral Small 3.2 24B | 56.2% | 50.0% | 41.7% | +8.3 pp |
| Qwen3 30B-A3B | 43.8% | 45.8% | 50.0% | −4.2 pp |
| Qwen3 4B | 50.0% | 35.4% | 35.4% | 0.0 pp |
| Llama 3.2 | 12.5% | 10.4% | 20.8% | −10.4 pp |

Topline interpretation: no evidence of the hypothesised pooled Rioplatense disadvantage in this bounded suite. Do not call this equivalence and do not generalise the pooled result to a model population. The model-specific directions are heterogeneous.

## Local/remote branch state and safe preservation

Documentation commits were added to `origin/lxai-rioplatense-experiment` after the local main process had started. The local checkout may therefore be behind origin while containing the untracked completed result directory.

Use:

```bash
git status
git pull --rebase origin lxai-rioplatense-experiment
git status
```

Then add **only** the completed main result directory:

```bash
git add research/lxai2026/results/20260825T094526Z
git commit -m "Store LXAI decision-complexity main results"
git push
```

Do not force-push. If a rebase conflict occurs, inspect it rather than automatically resolving or discarding experiment files.

## Files that define the experiment

- `suite_decision_complexity.json` — frozen 48-case suite.
- `PROTOCOL-EXPERIMENT-2.md` — current protocol and interpretation constraints.
- `run_decision_experiment.py` — decision-specific runner wrapper.
- `direct_ollama_client.py` — strict direct `/api/chat` client; no CLI/deterministic fallback.
- `run_experiment.py` — shared loading, scoring, randomisation, provenance and output machinery.
- `TRANSPORT-AUDIT-2026-08-25.md` — why the old Modelito-collected run was discarded.

## Transport provenance

The original Modelito 1.4.5 Ollama path did not transmit nominal `temperature`, `seed` or `num_predict` settings. The old 360-call dataset and its result summary were deleted.

Modelito commit `0bc829d2766a58e30b17c3822a9d70be9d038219` (`Fix Ollama settings propagation`) fixes the library issue, but this main LXAI run remains direct Ollama throughout to avoid mixing transports.

Direct payload contains:

- exact requested model;
- role-preserving system/user messages;
- `stream: false`;
- `options.temperature: 0.0`;
- `options.seed: 20260825 + repeat`;
- `options.num_predict: 24`.

## Pre-freeze diagnostics

Excluded from the main dataset:

- multiple smoke/debug runs;
- D1 four-state sanity runs;
- manual state-extraction/predicate/branch decomposition;
- manual branch-order control.

Key diagnostic finding: Qwen3 4B and 30B could read state and evaluate a simple threshold but showed an asymmetric false-branch policy-execution failure. Mistral executed both logically equivalent branch-order formulations correctly. Llama 3.2 additionally showed format-following failures in the symbolic probe. These observations motivated prospectively declared state-invariance/branch-position diagnostics, not a post-freeze prompt change.

## Required analysis after push

Verify from the exact machine-readable directory:

1. `metadata.json`, `results.csv`, `results.jsonl`, `summary.json` all present;
2. exactly 576 rows and zero provider errors;
3. model provenance/digests and suite/system-prompt hashes;
4. topline counts reproduced exactly.

Then derive:

- paired standard-vs-Rioplatense strict correctness by model, including discordant counts and exact McNemar p;
- paired results by D1–D6;
- validity, action-family, parameter and executable-consequence metrics;
- policy-complete correctness;
- state-invariant output;
- first-mentioned/branch-position selection against oracle branch frequencies.

Do not infer these from the console summary alone.

## Primary interpretation rule

The main inferential comparison is matched `es_standard` vs `es_rioplatense`. English is a secondary reference. D1–D6 absolute accuracy is not by itself a validated measure of reasoning complexity; interpret it with branch/state diagnostics.

Current safe statement:

> Within this controlled suite and these four fixed model conditions, the hypothesised pooled disadvantage for voseo-marked Rioplatense instructions was not observed. The pooled difference was small and model-specific effects changed direction.

Do not claim equivalence from nonsignificance, do not anthropomorphise a higher Rioplatense score, and do not generalise pooled results from these four fixed models to a model population.
